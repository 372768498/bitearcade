{
  "name": "gameweb",
  "version": "0.1.0",
  "private": true,
  "description": "Static HTML5 game portal that monetizes via GameMonetize / GameDistribution feeds.",
  "scripts": {
    "fetch": "node scripts/fetch-games.js",
    "build": "node scripts/build.js",
    "dev": "node scripts/serve.js",
    "start": "node scripts/build.js && node scripts/serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
